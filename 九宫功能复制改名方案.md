# PSD2UIForm 九宫功能升级方案（复制并改名）

## 🎯 目标

**复制 PSDLayoutTool2 的九宫模块到 PSD2UIForm，并改名避免冲突**，实现：
- ✅ PSDLayoutTool2 保持不变，继续独立工作
- ✅ PSD2UIForm 获得九宫功能升级
- ✅ 两个插件可以共存，互不干扰
- ✅ 用户可以选择使用哪个插件

---

## 📦 文件复制与改名策略

### 命名规则

```
PSDLayoutTool2 (源)         →    PSD2UIForm (目标)
====================              ====================
PsdNineSliceAnalyzer        →    Psd2UiNineSliceAnalyzer
PsdNineSliceNameRules       →    Psd2UiNineSliceNameRules
PsdNineSliceModels          →    Psd2UiNineSliceModels
PsdNineSliceBorder          →    Psd2UiNineSliceBorder
PsdNineSliceInference       →    Psd2UiNineSliceInference
PsdNineSliceConfidence      →    Psd2UiNineSliceConfidence
PsdNineSliceMode            →    Psd2UiNineSliceMode
...                         →    ...

命名空间:
PsdLayoutTool2              →    UGF.EditorTools.Psd2UGUI.NineSlice
```

### 前缀策略

**统一前缀**: `Psd2Ui` （区别于源代码的 `Psd`）

**理由**:
- 清晰标识来源（Psd2UI = PSD2UIForm）
- 避免与 PSDLayoutTool2 的类名冲突
- 保持命名一致性

---

## 🗂️ 文件清单与改名映射

### 核心文件（必需，共10个）

| 源文件 | 目标文件 | 核心类名变化 | 行数 |
|-------|---------|------------|------|
| `PsdNineSliceModels.cs` | `Psd2UiNineSliceModels.cs` | `PsdNineSliceBorder` → `Psd2UiNineSliceBorder`<br>`PsdNineSliceInference` → `Psd2UiNineSliceInference`<br>`PsdNineSliceConfidence` → `Psd2UiNineSliceConfidence` | 105 |
| `PsdNineSliceRaster.cs` | `Psd2UiNineSliceRaster.cs` | `PsdNineSliceRaster` → `Psd2UiNineSliceRaster` | 58 |
| `PsdNineSliceNameRules.cs` | `Psd2UiNineSliceNameRules.cs` | `PsdNineSliceNameRule` → `Psd2UiNineSliceNameRule`<br>`PsdNineSliceMode` → `Psd2UiNineSliceMode`<br>`PsdNineSliceNameRules` → `Psd2UiNineSliceNameRules` | 165 |
| `PsdNineSliceAnalyzer.cs` | `Psd2UiNineSliceAnalyzer.cs` | `PsdNineSliceAnalyzer` → `Psd2UiNineSliceAnalyzer` | 371 |
| `PsdNineSliceCropper.cs` | `Psd2UiNineSliceCropper.cs` | `PsdNineSliceCropper` → `Psd2UiNineSliceCropper` | 131 |
| `PsdNineSliceCropSafety.cs` | `Psd2UiNineSliceCropSafety.cs` | `PsdNineSliceCropSafety` → `Psd2UiNineSliceCropSafety` | 79 |
| `PsdNineSliceAutoProcessor.cs` | `Psd2UiNineSliceAutoProcessor.cs` | `PsdNineSliceUnityAutoProcessor` → `Psd2UiNineSliceUnityAutoProcessor` | 150 |
| `PsdNineSliceAutoProcessorCore.cs` | `Psd2UiNineSliceAutoProcessorCore.cs` | `PsdNineSliceAutoProcessor` → `Psd2UiNineSliceAutoProcessor` | 88 |
| `PsdNineSliceImportPolicy.cs` | `Psd2UiNineSliceImportPolicy.cs` | `PsdNineSliceImportPolicy` → `Psd2UiNineSliceImportPolicy` | 43 |
| `PsdNineSliceTextureProcessor.cs` | `Psd2UiNineSliceTextureProcessor.cs` | `PsdNineSliceTextureProcessor` → `Psd2UiNineSliceTextureProcessor` | 309 |

**小计**: ~1,499 行

### 可选文件（可视化编辑器，共4个）

| 源文件 | 目标文件 | 核心类名变化 | 行数 |
|-------|---------|------------|------|
| `PsdNineSliceWindow.cs` | `Psd2UiNineSliceWindow.cs` | `PsdNineSliceWindow` → `Psd2UiNineSliceWindow` | 822 |
| `PsdNineSliceOverrideStore.cs` | `Psd2UiNineSliceOverrideStore.cs` | `PsdNineSliceOverrideStore` → `Psd2UiNineSliceOverrideStore` | 207 |
| `PsdNineSlicePsdLayerSession.cs` | `Psd2UiNineSlicePsdLayerSession.cs` | `PsdNineSlicePsdLayerSession` → `Psd2UiNineSlicePsdLayerSession` | 149 |
| `PsdNineSliceAssetState.cs` | `Psd2UiNineSliceAssetState.cs` | `PsdNineSliceAssetState` → `Psd2UiNineSliceAssetState` | 183 |

**小计**: ~1,361 行

**总计**: ~2,860 行

---

## 🔧 自动化改名脚本

### PowerShell 脚本（Windows）

```powershell
# 文件: CopyAndRenameNineSlice.ps1

$sourceDir = "E:\Project\Demo\monsterhunter\Assets\UnityPSDLayoutTool2\Assets\PSDLayoutTool2\Editor\NineSlice"
$targetDir = "E:\Project\Demo\monsterhunter\Assets\UnityPSDLayoutTool2\Assets\PSD2UIForm\Src\Editor\Psd2UGUI\NineSlice"

# 创建目标目录
New-Item -ItemType Directory -Force -Path $targetDir | Out-Null

# 核心文件列表
$coreFiles = @(
    "PsdNineSliceModels.cs",
    "PsdNineSliceRaster.cs",
    "PsdNineSliceNameRules.cs",
    "PsdNineSliceAnalyzer.cs",
    "PsdNineSliceCropper.cs",
    "PsdNineSliceCropSafety.cs",
    "PsdNineSliceAutoProcessor.cs",
    "PsdNineSliceAutoProcessorCore.cs",
    "PsdNineSliceImportPolicy.cs",
    "PsdNineSliceTextureProcessor.cs"
)

# 可选文件列表（可视化编辑器）
$optionalFiles = @(
    "PsdNineSliceWindow.cs",
    "PsdNineSliceOverrideStore.cs",
    "PsdNineSlicePsdLayerSession.cs",
    "PsdNineSliceAssetState.cs"
)

# 是否包含可选文件
$includeOptional = $true

$allFiles = $coreFiles
if ($includeOptional) {
    $allFiles += $optionalFiles
}

foreach ($file in $allFiles) {
    $sourcePath = Join-Path $sourceDir $file
    $targetFileName = $file -replace "^Psd", "Psd2Ui"
    $targetPath = Join-Path $targetDir $targetFileName
    
    Write-Host "处理: $file -> $targetFileName"
    
    # 读取源文件
    $content = Get-Content $sourcePath -Raw -Encoding UTF8
    
    # 批量替换
    $replacements = @{
        # 命名空间
        "namespace PsdLayoutTool2" = "namespace UGF.EditorTools.Psd2UGUI.NineSlice"
        
        # 类名（按长度排序，避免部分替换）
        "PsdNineSliceUnityAutoProcessor" = "Psd2UiNineSliceUnityAutoProcessor"
        "PsdNineSliceAutoProcessorCore" = "Psd2UiNineSliceAutoProcessorCore"
        "PsdNineSliceAutoProcessor" = "Psd2UiNineSliceAutoProcessor"
        "PsdNineSliceTextureProcessor" = "Psd2UiNineSliceTextureProcessor"
        "PsdNineSlicePsdLayerSession" = "Psd2UiNineSlicePsdLayerSession"
        "PsdNineSliceOverrideStore" = "Psd2UiNineSliceOverrideStore"
        "PsdNineSliceImportPolicy" = "Psd2UiNineSliceImportPolicy"
        "PsdNineSliceCropSafety" = "Psd2UiNineSliceCropSafety"
        "PsdNineSliceAssetState" = "Psd2UiNineSliceAssetState"
        "PsdNineSliceConfidence" = "Psd2UiNineSliceConfidence"
        "PsdNineSliceInference" = "Psd2UiNineSliceInference"
        "PsdNineSliceNameRules" = "Psd2UiNineSliceNameRules"
        "PsdNineSliceNameRule" = "Psd2UiNineSliceNameRule"
        "PsdNineSliceAnalyzer" = "Psd2UiNineSliceAnalyzer"
        "PsdNineSliceCropper" = "Psd2UiNineSliceCropper"
        "PsdNineSliceWindow" = "Psd2UiNineSliceWindow"
        "PsdNineSliceRaster" = "Psd2UiNineSliceRaster"
        "PsdNineSliceBorder" = "Psd2UiNineSliceBorder"
        "PsdNineSliceMode" = "Psd2UiNineSliceMode"
    }
    
    foreach ($key in $replacements.Keys) {
        $content = $content -replace [regex]::Escape($key), $replacements[$key]
    }
    
    # 写入目标文件
    [System.IO.File]::WriteAllText($targetPath, $content, [System.Text.Encoding]::UTF8)
    
    Write-Host "  完成: $targetPath"
}

Write-Host "`n✅ 复制完成！共处理 $($allFiles.Count) 个文件"
Write-Host "目标目录: $targetDir"
```

### 使用方法

```powershell
# 1. 保存脚本为 CopyAndRenameNineSlice.ps1

# 2. 运行脚本
cd E:\Project\Demo\monsterhunter\Assets\UnityPSDLayoutTool2
.\CopyAndRenameNineSlice.ps1

# 3. 检查输出
# 文件应该出现在:
# Assets/PSD2UIForm/Src/Editor/Psd2UGUI/NineSlice/*.cs
```

---

## 📝 手动调整（脚本无法处理的部分）

### 1. 菜单路径（如果有）

```csharp
// Psd2UiNineSliceWindow.cs

// 修改前:
[MenuItem("Assets/PSD Layout/Analyze 9-Slice")]

// ✅ 修改后:
[MenuItem("Assets/PSD2UIForm/Analyze 9-Slice")]
```

### 2. 资源路径引用

检查是否有硬编码的路径，例如：
```csharp
// 如果有类似的路径引用，需要手动调整
"Assets/PSDLayoutTool2/..." → "Assets/PSD2UIForm/..."
```

### 3. 依赖的其他 PSDLayoutTool2 类

检查是否引用了九宫模块之外的类，例如：
```csharp
// 如果有，需要决定：
// 1. 也复制那些类
// 2. 或者重新实现
// 3. 或者移除该功能
```

---

## 🔌 集成到 PSD2UIForm

### 修改 UGUIParser.cs

```csharp
// 文件: UGUIParser.cs

// 添加 using
using UGF.EditorTools.Psd2UGUI.NineSlice;

// ========================================
// 删除旧代码（~175 行）
// ========================================

// ❌ 删除以下内容:
private int nineSliceBorderTolerance = 5;
internal int GetNineSliceBorderTolerance() { ... }
internal static Vector4 CalculateNineSliceBorder(...) { ... }
private static RangeInt FindNineSliceStretchRange(...) { ... }
private static void InitializeNineSliceChannelRanges(...) { ... }
private static bool TryExtendNineSliceRange(...) { ... }
private static Color32 GetNineSliceAxisPixel(...) { ... }

// ========================================
// ✅ 添加新实现（~50 行）
// ========================================

/// <summary>
/// 计算九宫边界（使用 PSDLayoutTool2 的三重推断算法）
/// </summary>
internal static Vector4 CalculateNineSliceBorder(Texture2D texture, string layerName = null)
{
    if (texture == null)
    {
        return Vector4.zero;
    }

    // 1. 尝试从图层名解析规则
    Psd2UiNineSliceNameRule rule = null;
    if (!string.IsNullOrEmpty(layerName))
    {
        // 兼容旧标签
        if (layerName.Contains("|sliced") || layerName.Contains("|九宫格") ||
            layerName.Contains("[sliced]") || layerName.Contains("[九宫格]"))
        {
            rule = new Psd2UiNineSliceNameRule(Psd2UiNineSliceMode.NineSlice, null);
        }
        else
        {
            Psd2UiNineSliceNameRules.TryParse(layerName, out rule);
        }

        // 如果有显式边界，直接返回
        if (rule != null && rule.HasExplicitBorder)
        {
            var border = rule.ExplicitBorder;
            return new Vector4(border.Left, border.Bottom, border.Right, border.Top);
        }
    }

    // 2. 使用三重推断算法
    Psd2UiNineSliceRaster raster = TextureToRaster(texture);
    Psd2UiNineSliceInference inference;
    
    if (Psd2UiNineSliceAnalyzer.TryInfer(raster, out inference))
    {
        var border = inference.Border;
        
        #if UNITY_EDITOR
        Debug.Log($"[Psd2UI 九宫推断] {layerName ?? texture.name}: " +
                  $"边界=({border.Left}, {border.Top}, {border.Right}, {border.Bottom}) " +
                  $"方法={inference.Method} 置信度={inference.Confidence}");
        #endif
        
        return new Vector4(border.Left, border.Bottom, border.Right, border.Top);
    }

    #if UNITY_EDITOR
    Debug.LogWarning($"[Psd2UI 九宫推断] {layerName ?? texture.name}: " +
                     $"自动推断失败，纹理可能太小或完全透明");
    #endif
    
    return Vector4.zero;
}

/// <summary>
/// 转换 Unity Texture2D 为九宫光栅数据
/// </summary>
private static Psd2UiNineSliceRaster TextureToRaster(Texture2D texture)
{
    Color32[] pixels = texture.GetPixels32();
    byte[] bytes = new byte[pixels.Length * 4];
    
    for (int i = 0; i < pixels.Length; i++)
    {
        bytes[i * 4 + 0] = pixels[i].r;
        bytes[i * 4 + 1] = pixels[i].g;
        bytes[i * 4 + 2] = pixels[i].b;
        bytes[i * 4 + 3] = pixels[i].a;
    }
    
    return new Psd2UiNineSliceRaster(texture.width, texture.height, bytes);
}

// ========================================
// 保留兼容性：旧函数签名
// ========================================

/// <summary>
/// 向后兼容：保留旧的函数签名
/// </summary>
internal static Vector4 CalculateNineSliceBorder(object value, byte value2 = 0, int value3 = -1)
{
    if (value == null)
    {
        return Vector4.zero;
    }
    
    // 转发到新实现（忽略旧的 alphaThreshold 和 tolerance 参数）
    return CalculateNineSliceBorder((Texture2D)value, layerName: null);
}
```

### 修改 Psd2UIFormConverter.cs

```csharp
// 文件: Psd2UIFormConverter.cs

using UGF.EditorTools.Psd2UGUI.NineSlice;

// ========================================
// 修改 EnsureNineSliceBorder（增加 layerName 参数）
// ========================================

internal static void EnsureNineSliceBorder(string texturePath, string layerName = null)
{
    TextureImporter importer = AssetImporter.GetAtPath(texturePath) as TextureImporter;
    if (importer == null)
    {
        return;
    }

    // 确保可读
    bool wasReadable = importer.isReadable;
    if (!wasReadable)
    {
        importer.isReadable = true;
        importer.SaveAndReimport();
    }

    // 加载纹理
    Texture2D texture = AssetDatabase.LoadAssetAtPath<Texture2D>(texturePath);
    if (texture == null)
    {
        return;
    }

    // ✅ 使用新算法，传递图层名
    Vector4 border = UGUIParser.CalculateNineSliceBorder(texture, layerName);
    
    if (border != Vector4.zero)
    {
        importer.spriteBorder = border;
        importer.SaveAndReimport();

        // 可选裁剪
        if (ScriptableSingleton<Psd2UIFormSettings>.Instance.AutoCropMinimalNineSlice)
        {
            TryCropMinimalNineSlice(texturePath, layerName);
        }
    }

    // 恢复可读性
    if (!wasReadable)
    {
        importer.isReadable = false;
        importer.SaveAndReimport();
    }
}

// ========================================
// 新增裁剪函数（使用 Psd2Ui 前缀的类）
// ========================================

private static bool TryCropMinimalNineSlice(string texturePath, string layerName)
{
    Texture2D texture = AssetDatabase.LoadAssetAtPath<Texture2D>(texturePath);
    if (texture == null)
    {
        return false;
    }

    // 解析命名规则
    Psd2UiNineSliceNameRule rule = null;
    if (!string.IsNullOrEmpty(layerName))
    {
        // 兼容旧标签
        if (layerName.Contains("|sliced") || layerName.Contains("|九宫格"))
        {
            rule = new Psd2UiNineSliceNameRule(Psd2UiNineSliceMode.NineSlice, null);
        }
        else
        {
            Psd2UiNineSliceNameRules.TryParse(layerName, out rule);
        }
    }
    
    if (rule == null)
    {
        rule = new Psd2UiNineSliceNameRule(Psd2UiNineSliceMode.NineSlice, null);
    }

    // 转换为光栅
    Psd2UiNineSliceRaster raster = UGUIParser.TextureToRaster(texture);

    // 使用裁剪处理器
    Psd2UiNineSliceRaster croppedRaster;
    Psd2UiNineSliceBorder border;
    string reason;
    
    if (Psd2UiNineSliceAutoProcessor.TryProcessRaster(
        raster,
        rule,
        out croppedRaster,
        out border,
        out reason))
    {
        // 转换回 Texture2D
        Texture2D croppedTexture = new Texture2D(
            croppedRaster.Width, 
            croppedRaster.Height, 
            TextureFormat.RGBA32, 
            false);
        
        Color32[] croppedPixels = new Color32[croppedRaster.Width * croppedRaster.Height];
        for (int i = 0; i < croppedPixels.Length; i++)
        {
            croppedPixels[i] = new Color32(
                croppedRaster.Pixels[i * 4 + 0],
                croppedRaster.Pixels[i * 4 + 1],
                croppedRaster.Pixels[i * 4 + 2],
                croppedRaster.Pixels[i * 4 + 3]);
        }
        
        croppedTexture.SetPixels32(croppedPixels);
        byte[] pngBytes = croppedTexture.EncodeToPNG();
        UnityEngine.Object.DestroyImmediate(croppedTexture);

        // 写入文件
        System.IO.File.WriteAllBytes(texturePath, pngBytes);
        AssetDatabase.Refresh();

        // 更新边界
        TextureImporter importer = AssetImporter.GetAtPath(texturePath) as TextureImporter;
        if (importer != null)
        {
            importer.spriteBorder = new Vector4(
                border.Left, 
                border.Bottom, 
                border.Right, 
                border.Top);
            importer.SaveAndReimport();
        }

        Debug.Log($"[Psd2UI 九宫裁剪] {texturePath}: 裁剪成功");
        return true;
    }
    else
    {
        Debug.LogWarning($"[Psd2UI 九宫裁剪] {texturePath}: {reason}");
        return false;
    }
}

// ========================================
// 向后兼容：保留旧函数签名
// ========================================

internal static void EnsureNineSliceBorder(object text)
{
    EnsureNineSliceBorder((string)text, layerName: null);
}
```

### 修改调用点（传递图层名）

```csharp
// 在 UGUIParser.cs 或其他生成 UI 的地方

// 修改前:
Psd2UIFormConverterEditor.EnsureNineSliceBorder(texturePath);

// ✅ 修改后（传递图层名）:
Psd2UIFormConverterEditor.EnsureNineSliceBorder(texturePath, layer.Name);
```

---

## 🎨 更新 Photoshop 脚本

```javascript
// 文件: PSD2UGUI-LayerTagMenu.jsx

// ========================================
// 九宫格菜单项（更新）
// ========================================

// 旧标签（保留兼容）
menuItems.push({
    label: "图像类型: 九宫格 (旧格式)",
    tag: "|sliced",
    description: "向后兼容，自动转换为新算法"
});

// ✅ 新增标签
menuItems.push({
    label: "图像类型: 九宫格 (自动推断) ⭐推荐",
    tag: "|9slice",
    description: "使用三重推断算法（视觉边缘+重复检测+圆角保护）"
});

menuItems.push({
    label: "图像类型: 九宫格 (显式边界)",
    tag: "|9slice=10,10,10,10",
    description: "手动指定边界值（Left,Top,Right,Bottom）"
});

menuItems.push({
    label: "图像类型: 横向三切 (进度条)",
    tag: "|h3slice",
    description: "只拉伸水平方向，保留完整高度"
});

menuItems.push({
    label: "图像类型: 纵向三切 (装饰条)",
    tag: "|v3slice",
    description: "只拉伸垂直方向，保留完整宽度"
});

menuItems.push({
    label: "图像类型: 九宫格 (中文标签)",
    tag: "|jiugong",
    description: "Figma 兼容标签"
});
```

---

## ✅ 验证清单

### 编译检查
- [ ] Unity 编译通过
- [ ] 无类名冲突（Psd2Ui 前缀区分）
- [ ] 无命名空间冲突
- [ ] 两个插件可以同时存在

### 功能测试（PSD2UIForm）
- [ ] 旧标签 `|sliced` 使用新算法
- [ ] 新标签 `|9slice` 正常推断
- [ ] 显式边界 `|9slice=L,T,R,B` 正确应用
- [ ] 三切模式正常工作
- [ ] 圆角矩形边界正确
- [ ] 自动裁剪功能正常

### 独立性测试
- [ ] PSDLayoutTool2 功能不受影响
- [ ] 可以单独使用 PSDLayoutTool2
- [ ] 可以单独使用 PSD2UIForm
- [ ] 两个插件不互相干扰

---

## 📊 最终效果

### 目录结构

```
Assets/
├── PSDLayoutTool2/              ← 保持不变
│   └── Editor/
│       └── NineSlice/
│           ├── PsdNineSliceAnalyzer.cs
│           ├── PsdNineSliceNameRules.cs
│           └── ... (原始文件)
│
└── PSD2UIForm/                  ← 升级
    └── Src/Editor/Psd2UGUI/
        └── NineSlice/           ← 新增目录
            ├── Psd2UiNineSliceAnalyzer.cs      (复制+改名)
            ├── Psd2UiNineSliceNameRules.cs     (复制+改名)
            └── ... (所有文件都加 Psd2Ui 前缀)
```

### 类名对照表

| PSDLayoutTool2 | PSD2UIForm | 说明 |
|---------------|-----------|------|
| `PsdNineSliceAnalyzer` | `Psd2UiNineSliceAnalyzer` | 三重推断引擎 |
| `PsdNineSliceNameRules` | `Psd2UiNineSliceNameRules` | 命名规则解析 |
| `PsdNineSliceBorder` | `Psd2UiNineSliceBorder` | 边界数据模型 |
| `PsdNineSliceWindow` | `Psd2UiNineSliceWindow` | 可视化编辑器 |

**无冲突，可共存！**

---

## ⏱️ 实施时间

- **第1步**：运行脚本复制文件（10 分钟）
- **第2步**：手动调整菜单路径等（30 分钟）
- **第3步**：修改 UGUIParser 调用点（2 小时）
- **第4步**：测试验证（4 小时）

**总计**: 1 天

---

## 🚀 下一步

我可以立即为你：

1. ✅ 生成完整的 PowerShell 复制脚本
2. ✅ 生成修改后的 `UGUIParser.cs` 代码
3. ✅ 生成修改后的 `Psd2UIFormConverter.cs` 代码
4. ✅ 列出所有需要手动检查的点

**需要我开始吗？** 🚀
