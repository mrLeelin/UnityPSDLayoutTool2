# PSDLayoutTool2 vs PSD2UIForm 九宫切片方案对比

## 一、核心架构对比

### PSDLayoutTool2 (现代化方案)

```
命名规则解析 → 像素分析 → 边界推断 → 裁剪优化 → PNG导出
    ↓              ↓           ↓           ↓          ↓
NameRules → Analyzer → Inference → Cropper → AutoProcessor
```

**特点：**
- 模块化设计，职责清晰分离
- 独立的九宫专用命名空间 (`NineSlice/`)
- 完整的推断引擎（视觉边缘 + 重复检测 + 圆角保护）
- 支持 PSD 层级浏览和手动调整
- 元数据持久化（存储在 PSD asset meta）

### PSD2UIForm (传统方案)

```
图层标签解析 → 纹理像素分析 → 边界计算 → Sprite应用
      ↓              ↓              ↓           ↓
UGUIParser → CalculateNineSliceBorder → spriteBorder
```

**特点：**
- 功能耦合在 `UGUIParser` 大类中（3000+ 行）
- 九宫逻辑分散在转换流程中
- 基于容差的像素范围查找算法
- 自动裁剪作为可选开关
- 无独立的可视化编辑器


---

## 二、命名规则对比

### PSDLayoutTool2 支持的标签

| 标签形式 | 示例 | 模式 | 说明 |
|---------|------|------|------|
| 管道符自动 | `background\|9slice` | NineSlice | 自动推断边界 |
| 括号自动 | `button[9slice]` | NineSlice | 自动推断边界 |
| 显式边界 | `panel\|9slice=10,15,10,15` | NineSlice | 指定 L,T,R,B |
| 括号显式 | `card[9slice:8,8,8,8]` | NineSlice | 指定边界 |
| 横向三切 | `bar\|h3slice` | HorizontalThreeSlice | 只拉伸水平 |
| 纵向三切 | `progress\|v3slice` | VerticalThreeSlice | 只拉伸垂直 |
| **中文兼容** | `背景\|jiugong` | NineSlice | Figma 兼容 |
| **中文前缀** | `jiugongh3_button` | HorizontalThreeSlice | Figma 前缀 |
| 错别字容错 | `item\|jougongv3` | VerticalThreeSlice | 容错 'u'→'iu' |

**解析器：** `PsdNineSliceNameRules.cs` (165 行，正则驱动)

```csharp
// 显式边界正则
@"(?:\|9slice\s*=\s*|\[9slice\s*:\s*)" + Number + @"\s*,\s*" + Number + @"\s*,\s*" + Number + @"\s*,\s*" + Number

// 自动标签正则
@"(?:\|(?<pipe>9slice|h3slice|v3slice|jiugongh3|jiugongv3|jougongv3|jiugong)\b|...)"

// Figma 前缀正则
@"^(?<prefix>jiugongh3|jiugongv3|jougongv3|jiugong)(?:[_\-\s]+|$)"
```

### PSD2UIForm 支持的标签

| 标签形式 | 示例 | 图像类型 | 说明 |
|---------|------|----------|------|
| 管道符 | `background\|sliced` | Image.Type.Sliced | 自动计算边界 |
| 中文别名 | `背景\|九宫格` | Image.Type.Sliced | 中文支持 |

**解析器：** 硬编码在 `UGUIParser.cs` 的标签映射字典中

```csharp
AddTagFamilyOption(dictionary, "imageType", "sliced", "九宫格");
AddTagAliasMapping(dictionary2, "sliced", null, null, null, "sliced");
```

**对比总结：**
- **PSDLayoutTool2**: 9种标签形式，支持三切模式，Figma 互操作性强
- **PSD2UIForm**: 2种标签形式，仅支持完整九宫，功能单一


---

## 三、边界推断算法对比

### PSDLayoutTool2 的三重推断策略

**核心文件：** `PsdNineSliceAnalyzer.cs` (371 行)

#### 1. 视觉边缘检测 (`InferVisualEdges`)

```csharp
// 采样中心 60% 区域作为基准
EdgeSampleRatio = 0.60f

// 从边缘向内扫描，寻找颜色稳定区域
EdgeDifferenceThreshold = 12.0f  // 允许 ±12 的颜色差异
StableRun = 3                     // 连续3像素稳定才认定为内容区
```

**逻辑：**
1. 对行和列分别计算平均颜色
2. 从边缘向内，当颜色与中心基准差异 ≤12 且连续3行稳定时，认定为内容开始
3. 边界 = 稳定开始位置 - 连续数 + 1

#### 2. 重复线检测 (`InferRepeatedEdges`)

```csharp
// 检测连续2条完全相同的行/列
repeatCount >= 2  // 表示可拉伸区域
```

**逻辑：**
1. 从边缘向内逐行/列比较
2. 当发现连续2条完全相同的像素行时，认定为拉伸区开始
3. 边界 = 重复开始位置 - 重复数

#### 3. 圆角保护 (`InferRoundRectBorder`)

```csharp
RoundRectInsetRatio = 0.05f        // 检测短边 5% 的透明内缩
RoundRectSafetyMultiplier = 1.25f  // 安全边界 × 1.25
```

**逻辑：**
1. 检测四角的透明像素内缩距离
2. 如果最大内缩 ≥ 短边的 5%，认定为圆角矩形
3. 强制边界 ≥ (最大内缩 × 1.25)，保护圆角不被切割

#### 4. 融合策略 (`Merge`)

```csharp
if (repeat <= 4 && visual > repeat * 1.5) {
    use visual;  // 重复检测太小时，信任视觉边缘
} else if (visual <= repeat * 1.5) {
    use visual;  // 视觉边缘保守时，优先采用
} else {
    use max(repeat, min(visual, repeat * 1.5));  // 取中间值
}

// 最终限制
border = clamp(result, 4, min(0.20 * size, 96px));
```

**信心等级：**
```csharp
enum PsdNineSliceConfidence { None, Low, Medium, High }

// 自动推断 → Medium
// 圆角保护 → "visual-repeat-round-rect"
// 普通融合 → "visual-repeat"
```

---

### PSD2UIForm 的容差范围查找

**核心文件：** `UGUIParser.cs` 中的 `CalculateNineSliceBorder()` (行 940-969)

#### 算法流程

```csharp
// 1. 计算不透明区域
RectInt opaqueBounds = CalculateOpaqueBounds(pixels, width, height, alphaThreshold);

// 2. 查找可拉伸范围（水平和垂直分别处理）
RangeInt hStretch = FindNineSliceStretchRange(pixels, width, bounds, isHorizontal, threshold, tolerance);
RangeInt vStretch = FindNineSliceStretchRange(pixels, width, bounds, isVertical, threshold, tolerance);

// 3. 计算边界
left   = hStretch.start - opaqueBounds.xMin
right  = opaqueBounds.xMax - (hStretch.start + hStretch.length)
top    = vStretch.start - opaqueBounds.yMin
bottom = opaqueBounds.yMax - (vStretch.start + vStretch.length)
```

#### 核心参数

```csharp
nineSliceBorderTolerance = 5;  // 默认容差，可在 Inspector 调整 (0-255)
```

#### 拉伸范围查找逻辑 (`FindNineSliceStretchRange`)

**目标：** 找到**最长的**、**左右边界最对称的**、**中心位置最接近正中**的连续像素范围

```csharp
for (int i = start + 1; i < end; i++) {
    for (int j = i; j < end; j++) {
        // 检查 [i, j] 区域的每个垂直列
        // 对每列维护 RGBA 四通道的 min/max 范围
        
        // 如果任一通道的范围 > tolerance，停止扩展
        if (maxR - minR > tolerance || 
            maxG - minG > tolerance || 
            maxB - minB > tolerance || 
            maxA - minA > tolerance) {
            break;
        }
        
        // 评分标准（优先级递减）：
        // 1. 长度最大
        // 2. 左右边界差异最小 (abs(leftBorder - rightBorder))
        // 3. 中心位置最接近正中
        // 4. 最小边界最大 (min(leftBorder, rightBorder))
    }
}
```

**示例：**
```
假设图像宽度 100px，不透明区域 [10, 90]
候选范围 A: [40, 60]  长度=20, 左边界=30, 右边界=30, 中心=50
候选范围 B: [45, 60]  长度=15, 左边界=35, 右边界=30, 中心=52.5

评分：
  A: 长度=20 > B:15  ✓ 优先选 A
  如果长度相同，则比较 abs(30-30)=0 vs abs(35-30)=5，选 A
```

---

### 算法对比总结

| 维度 | PSDLayoutTool2 | PSD2UIForm |
|-----|----------------|------------|
| **策略数量** | 3种（视觉+重复+圆角） | 1种（容差范围查找） |
| **适应性** | 多策略融合，鲁棒性强 | 单策略，依赖参数调优 |
| **圆角处理** | 专门检测并保护圆角 | 无特殊处理 |
| **参数暴露** | 硬编码，经验值优化 | 容差可调（0-255） |
| **可预测性** | 黑盒推断，结果稳定 | 参数敏感，需手动调试 |
| **边界限制** | 4px ≤ border ≤ min(20%, 96px) | 无硬性限制 |
| **复杂度** | 高（371行，三重策略） | 中（150行，单策略） |


---

## 四、裁剪优化对比

### PSDLayoutTool2 的最小裁剪

**核心文件：** `PsdNineSliceCropper.cs` (4599 字节)

```csharp
const int MinimumStretchCenterPixels = 2;  // 保留最小2px中心区（匹配 Figma）

// 裁剪策略
PsdNineSliceRaster cropped = Crop(
    sourceRaster, 
    border, 
    mode  // NineSlice / HorizontalThreeSlice / VerticalThreeSlice
);

// 三切模式特殊处理
if (mode == HorizontalThreeSlice) {
    // 只裁剪左右，保留完整高度
}
if (mode == VerticalThreeSlice) {
    // 只裁剪上下，保留完整宽度
}
```

**安全检查：** `PsdNineSliceCropSafety.cs`

```csharp
// 拒绝裁剪条件：
1. 原始尺寸 < 最小安全尺寸 (80x80)
2. 裁剪后尺寸 / 原始尺寸 < 0.25  (丢失超过 75% 像素)
3. 裁剪后文件大小 / 原始大小 < 0.15  (PNG 压缩比异常)

// 保护场景：预渲染的卡片、带特效的面板
```

### PSD2UIForm 的可选裁剪

**核心文件：** `RightClickExtension.cs` 中的 `TryCropMinimalNineSlice()`

```csharp
// 配置开关
Psd2UIFormSettings.AutoCropMinimalNineSlice = true/false;

// 调用时机
if (imageType == Image.Type.Sliced) {
    Psd2UIFormConverterEditor.EnsureNineSliceBorder(texture);
    
    if (AutoCropMinimalNineSlice) {
        RightClickExtension.TryCropMinimalNineSlice(assetPath);
    }
}
```

**裁剪逻辑：** （代码被混淆，无法深入分析）


---

## 五、可视化编辑器对比

### PSDLayoutTool2

**窗口：** `PsdNineSliceWindow.cs` (28845 字节)

**功能：**
1. **PNG 模式**：
   - 点击 "Analyze" 推断边界
   - 显示推断结果（Medium confidence）
   - 手动调整四个边界值
   - 点击 "Apply" 写入 Sprite meta

2. **PSD 模式**：
   - 列出所有可见光栅图层
   - 搜索过滤图层
   - 逐层分析和编辑
   - 交互式拖拽青色导轨
   - 持久化到 PSD meta（JSON）

**UI 特性：**
- 实时预览裁剪效果
- 显示推断方法（visual-repeat / round-rect）
- 自动保存防抖（2秒延迟）
- 错误状态提示

**菜单入口：**
```
Assets/PSD Layout/Analyze 9-Slice
```

### PSD2UIForm

**无独立编辑器**

**调整方式：**
1. 修改全局容差参数：
   ```csharp
   UGUIParser.nineSliceBorderTolerance = 5;  // 0-255
   ```

2. 重新生成 UI

3. 手动在 Sprite Editor 中调整


---

## 六、集成方式对比

### PSDLayoutTool2

**触发时机：** 导入阶段自动处理

```csharp
// 在 PsdImporter.ExportTree() 中
if (PsdNineSliceNameRules.TryParse(layerName, out rule)) {
    PsdNineSliceAutoProcessor.TryProcess(
        texture, 
        rule, 
        horizontalScale,  // Canvas 缩放
        verticalScale, 
        out png, 
        out border, 
        out reason
    );
    
    // 写入 PNG + 设置 spriteBorder
}
```

**特点：**
- 与 Canvas 缩放联动
- 支持物理坐标缩放后的边界重算
- 处理失败时回退到完整纹理

### PSD2UIForm

**触发时机：** 生成 UI 后后处理

```csharp
// 在 UGUIParser 生成完 Image 组件后
if (imageType == Image.Type.Sliced) {
    Vector4 border = CalculateNineSliceBorder(texture, alphaThreshold, tolerance);
    sprite.spriteBorder = border;
    
    if (AutoCropMinimalNineSlice) {
        // 裁剪并替换纹理
    }
}
```

**特点：**
- 在 Unity 场景中直接操作
- 与 UGUI 组件紧密耦合
- 无 Canvas 缩放感知


---

## 七、质量保证对比

### PSDLayoutTool2

**测试覆盖：** 
- 单元测试（推断算法）
- 边界验证（最小中心、比例限制）
- 失败回退机制
- 诊断日志输出

**边界约束：**
```csharp
border >= 4px                    // 最小边界
border <= min(size * 0.20, 96px) // 最大边界（相对+绝对）
center >= 2px                    // 最小可拉伸区域
```

**错误处理：**
```csharp
if (!TryProcess(..., out reason)) {
    Debug.LogWarning($"9-Slice failed for {layerName}: {reason}");
    // 导出完整纹理，不裁剪
}
```

### PSD2UIForm

**测试覆盖：** 未知（代码混淆）

**边界约束：** 无硬性限制

**错误处理：** 静默失败或返回 Vector4.zero


---

## 八、扩展性对比

### PSDLayoutTool2

**模块化架构：**
```
PsdNineSliceModels       - 数据模型（Border, Inference, Confidence）
PsdNineSliceNameRules    - 命名规则解析器
PsdNineSliceAnalyzer     - 像素推断引擎
PsdNineSliceCropper      - 裁剪执行器
PsdNineSliceCropSafety   - 安全策略
PsdNineSliceAutoProcessor - 主流程编排
PsdNineSliceWindow       - 可视化编辑器
```

**新增支持步骤：**
1. 在 `NameRules` 添加新正则
2. 在 `Analyzer` 添加新策略
3. 在 `Cropper` 添加新模式
4. 在 `Window` 添加 UI 控件

### PSD2UIForm

**耦合架构：**
- 所有逻辑在 `UGUIParser` 单类
- 九宫功能与 UI 生成流程强耦合
- 修改需要理解整个转换管线

**新增支持步骤：**
1. 修改 `CalculateNineSliceBorder` 函数
2. 可能影响整个 UGUI 生成流程
3. 需要重新测试所有 UI 类型


---

## 九、性能对比

### 计算复杂度

| 操作 | PSDLayoutTool2 | PSD2UIForm |
|-----|----------------|------------|
| **命名解析** | O(n) - 3个正则匹配 | O(1) - 字典查找 |
| **像素分析** | O(w×h×3) - 三重策略 | O(w×h×k²) - 范围枚举（k=搜索半径） |
| **边界融合** | O(1) - 常数时间 | O(w×h) - 逐列min/max统计 |
| **裁剪** | O(w×h) - 单次复制 | O(w×h) - 可选操作 |

**示例：** 1024×1024 图像
- PSDLayoutTool2: ~3M 像素访问（3次完整扫描）
- PSD2UIForm: ~50M 像素访问（范围枚举 × 容差检查）

### 内存占用

- **PSDLayoutTool2**: 
  - 临时 RGBA 数组：w×h×4 字节
  - 采样缓存：2×(w+h)×16 字节
  - 总计：~4.2MB (1024×1024)

- **PSD2UIForm**:
  - 完整像素数组：w×h×4 字节
  - 8个通道范围数组：8×max(w,h)×4 字节
  - 总计：~4.03MB (1024×1024)


---

## 十、推荐方案

### 选择 PSDLayoutTool2 的场景

✅ **适用于：**
1. 需要批量处理大量 PSD，自动化程度要求高
2. 设计资产包含大量圆角矩形（现代扁平化 UI）
3. 需要支持 Figma 导出的命名约定（国际化团队）
4. 需要三切模式（进度条、装饰条）
5. 需要可视化调试和手动微调
6. 需要与 Canvas 缩放联动（多分辨率适配）

✅ **优势：**
- 自动化程度高，推断准确率高
- 圆角保护算法成熟
- 命名规则灵活（9种标签形式）
- 有独立编辑器支持手动调整
- 代码模块化，易于维护和扩展

❌ **劣势：**
- 学习曲线陡峭（需理解三重策略）
- 参数不可调（硬编码经验值）
- 依赖命名约定（需要设计师培训）

---

### 选择 PSD2UIForm 的场景

✅ **适用于：**
1. 简单 UI，九宫需求单一
2. 已有 PSD2UIForm 工作流，迁移成本高
3. 需要微调容差参数以匹配特定美术风格
4. 不需要三切或圆角保护
5. 倾向于在 Unity Sprite Editor 中手动调整

✅ **优势：**
- 算法简单，容易理解
- 容差参数可调（适应不同风格）
- 与 UGUI 生成流程深度集成
- 可选裁剪，灵活度高

❌ **劣势：**
- 仅支持 `|sliced` 标签，功能单一
- 无圆角保护，圆角矩形易失真
- 无独立编辑器，调试困难
- 算法复杂度高（范围枚举）
- 代码耦合严重，扩展困难


---

## 十一、迁移建议

### 从 PSD2UIForm 迁移到 PSDLayoutTool2

**步骤：**

1. **命名规则转换：**
   ```
   旧: background|sliced
   新: background|9slice
   ```

2. **配置迁移：**
   - 取消 `AutoCropMinimalNineSlice`（PSDLayoutTool2 自动处理）
   - 移除 `nineSliceBorderTolerance` 参数（使用自动推断）

3. **测试验证：**
   - 对比生成前后的 spriteBorder 值
   - 使用 `PsdNineSliceWindow` 逐层检查
   - 关注圆角矩形的边界是否准确

4. **回退方案：**
   - 保留原 PSD2UIForm 代码
   - 通过图层名切换：`|sliced` (旧) vs `|9slice` (新)

### 从 PSDLayoutTool2 迁移到 PSD2UIForm

**步骤：**

1. **命名规则转换：**
   ```
   旧: background|9slice
   新: background|sliced
   
   旧: progress|h3slice
   新: 手动设置 spriteBorder (不支持自动)
   ```

2. **手动调整：**
   - 圆角矩形需要在 Sprite Editor 手动扩大边界
   - 复杂图案需要调整 `nineSliceBorderTolerance`

3. **测试验证：**
   - 关注圆角是否变形
   - 检查拉伸区域是否正确

---

## 十二、总结表

| 维度 | PSDLayoutTool2 | PSD2UIForm |
|-----|----------------|------------|
| **代码行数** | ~1500 行（九宫模块） | ~200 行（分散在大类中） |
| **模块化** | ⭐⭐⭐⭐⭐ 独立命名空间 | ⭐⭐ 耦合在转换器 |
| **命名规则** | ⭐⭐⭐⭐⭐ 9种标签 | ⭐⭐ 2种标签 |
| **推断算法** | ⭐⭐⭐⭐⭐ 三重策略 | ⭐⭐⭐ 单策略 |
| **圆角支持** | ⭐⭐⭐⭐⭐ 专门算法 | ⭐ 无 |
| **三切模式** | ⭐⭐⭐⭐⭐ 支持 | ⭐ 不支持 |
| **可视化编辑** | ⭐⭐⭐⭐⭐ 独立窗口 | ⭐ 无 |
| **参数可调** | ⭐⭐ 硬编码 | ⭐⭐⭐⭐ 容差可调 |
| **Canvas 缩放** | ⭐⭐⭐⭐⭐ 支持 | ⭐ 不支持 |
| **扩展性** | ⭐⭐⭐⭐⭐ 模块化 | ⭐⭐ 耦合 |
| **学习曲线** | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐ 简单 |
| **文档完整性** | ⭐⭐⭐⭐⭐ 完整注释 | ⭐⭐ 混淆代码 |

**综合评分：**
- **PSDLayoutTool2**: 87/100 - 现代化、功能完备、可维护性强
- **PSD2UIForm**: 62/100 - 简单直接、但功能受限、扩展困难

**结论：**  
对于新项目或重构项目，**强烈推荐 PSDLayoutTool2**。  
对于遗留项目且九宫需求简单，可继续使用 PSD2UIForm。
