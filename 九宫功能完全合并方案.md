# PSD2UIForm 与 PSDLayoutTool2 完全合并方案

## 🎯 目标

**完全合并两个插件为一个统一的 PSD 导入工具**，最终只保留一套代码库。

---

## 🤔 合并决策：保留哪个作为基础？

### 选项 A：以 PSDLayoutTool2 为基础，整合 PSD2UIForm 的功能

**优势：**
- ✅ PSDLayoutTool2 代码质量更高（模块化、有文档、无混淆）
- ✅ 九宫功能已经是最优实现（三重策略、圆角保护）
- ✅ 有完整的可视化编辑器
- ✅ 架构清晰，易于扩展

**需要整合：**
- PSD2UIForm 的 UGUI 生成功能（核心价值）
- PSD2UIForm 的图层标签系统（|Button、|Image、|Text 等）
- PSD2UIForm 的 Photoshop 脚本（自动标注）
- PSD2UIForm 的 AI 辅助功能

**工作量：** 中等（需要移植 PSD2UIForm 的 UI 生成管线）

---

### 选项 B：以 PSD2UIForm 为基础，替换九宫模块

**优势：**
- ✅ PSD2UIForm 已经有完整的 UGUI 生成流程
- ✅ 有 Photoshop 脚本生态（自动标注、导出）
- ✅ 有 AI 辅助布局功能（独特卖点）
- ✅ 用户可能已经在使用 PSD2UIForm

**需要替换：**
- PSD2UIForm 的九宫算法（~175 行） → PSDLayoutTool2 的九宫模块（~1500 行）
- 同时保留 PSD2UIForm 的其他功能

**工作量：** 低（只替换九宫部分）

---

## ✅ 推荐方案：选项 B（以 PSD2UIForm 为基础）

**理由：**
1. **最小化改动**：只替换九宫模块，其他功能保持不变
2. **保留核心价值**：PSD2UIForm 的 UGUI 生成 + AI 辅助 + Photoshop 集成
3. **向后兼容**：现有用户无需重新学习
4. **快速落地**：1-2 天即可完成

---

## 📦 具体实施方案

### 第一步：将 PSDLayoutTool2 的九宫模块复制到 PSD2UIForm

```bash
# 源目录
Assets/PSDLayoutTool2/Editor/NineSlice/
├── PsdNineSliceAnalyzer.cs          (371 行 - 三重推断引擎)
├── PsdNineSliceAutoProcessor.cs     (150 行 - 主流程编排)
├── PsdNineSliceAutoProcessorCore.cs (88 行 - 核心逻辑)
├── PsdNineSliceBorder.cs            (已在 Models 中)
├── PsdNineSliceCropper.cs           (131 行 - 裁剪器)
├── PsdNineSliceCropSafety.cs        (79 行 - 安全检查)
├── PsdNineSliceImportPolicy.cs      (43 行 - 导入策略)
├── PsdNineSliceModels.cs            (105 行 - 数据模型)
├── PsdNineSliceNameRules.cs         (165 行 - 命名规则)
├── PsdNineSliceOverrideStore.cs     (207 行 - 手动覆盖存储)
├── PsdNineSlicePsdLayerSession.cs   (149 行 - PSD 层会话)
├── PsdNineSliceRaster.cs            (58 行 - 光栅数据)
├── PsdNineSliceTextureProcessor.cs  (309 行 - 纹理处理)
└── PsdNineSliceWindow.cs            (822 行 - 可视化编辑器)

# 目标目录
Assets/PSD2UIForm/Src/Editor/Psd2UGUI/NineSlice/  ← 新建
├── [复制上述所有文件]
└── [修改命名空间]
```

**操作：**
```bash
# 1. 复制整个 NineSlice 目录
cp -r Assets/PSDLayoutTool2/Editor/NineSlice/* \
      Assets/PSD2UIForm/Src/Editor/Psd2UGUI/NineSlice/

# 2. 批量修改命名空间
# 查找: namespace PsdLayoutTool2
# 替换: namespace UGF.EditorTools.Psd2UGUI.NineSlice
```

---

### 第二步：删除 PSD2UIForm 的旧九宫代码

```csharp
// UGUIParser.cs - 删除以下内容：

// ❌ 删除字段
private int nineSliceBorderTolerance = 5;  // 行 221

// ❌ 删除方法（共 ~175 行）
internal int GetNineSliceBorderTolerance()           // 行 300-303
internal static Vector4 CalculateNineSliceBorder()   // 行 940-969
private static RangeInt FindNineSliceStretchRange()  // 行 971-1051
private static void InitializeNineSliceChannelRanges() // 行 1053-1064
private static bool TryExtendNineSliceRange()        // 行 1066-1091
private static Color32 GetNineSliceAxisPixel()       // 行 1106-1115
```

---

### 第三步：修改调用点（无需适配器）

```csharp
// ========================================
// UGUIParser.cs
// ========================================

// 修改前：
internal static Vector4 CalculateNineSliceBorder(object value, byte value2 = 0, int value3 = -1)
{
    // 旧的容差算法（~30 行）
    if (value == null) return Vector4.zero;
    // ... 复杂的范围查找 ...
    return new Vector4(left, bottom, right, top);
}

// ✅ 修改后：
internal static Vector4 CalculateNineSliceBorder(Texture2D texture, string layerName = null)
{
    if (texture == null)
    {
        return Vector4.zero;
    }

    // 1. 尝试从图层名解析显式边界
    PsdNineSliceNameRule rule = null;
    if (!string.IsNullOrEmpty(layerName))
    {
        // 兼容旧标签 |sliced
        if (layerName.Contains("|sliced") || layerName.Contains("|九宫格"))
        {
            rule = new PsdNineSliceNameRule(PsdNineSliceMode.NineSlice, null);
        }
        else
        {
            PsdNineSliceNameRules.TryParse(layerName, out rule);
        }

        // 如果有显式边界，直接返回
        if (rule != null && rule.HasExplicitBorder)
        {
            var border = rule.ExplicitBorder;
            return new Vector4(border.Left, border.Bottom, border.Right, border.Top);
        }
    }

    // 2. 使用三重推断算法
    PsdNineSliceRaster raster = TextureToRaster(texture);
    PsdNineSliceInference inference;
    
    if (PsdNineSliceAnalyzer.TryInfer(raster, out inference))
    {
        var border = inference.Border;
        Debug.Log($"[九宫推断] {layerName ?? texture.name}: " +
                  $"边界=({border.Left},{border.Top},{border.Right},{border.Bottom}) " +
                  $"方法={inference.Method} 置信度={inference.Confidence}");
        
        return new Vector4(border.Left, border.Bottom, border.Right, border.Top);
    }

    Debug.LogWarning($"[九宫推断] {layerName ?? texture.name}: 推断失败，返回零边界");
    return Vector4.zero;
}

// 辅助函数：转换纹理为光栅
private static PsdNineSliceRaster TextureToRaster(Texture2D texture)
{
    Color32[] pixels = texture.GetPixels32();
    byte[] bytes = new byte[pixels.Length * 4];
    
    for (int i = 0; i < pixels.Length; i++)
    {
        bytes[i * 4 + 0] = pixels[i].r;
        bytes[i * 4 + 1] = pixels[i].g;
        bytes[i * 4 + 2] = pixels[i].b;
        bytes[i * 4 + 3] = pixels[i].a;
    }
    
    return new PsdNineSliceRaster(texture.width, texture.height, bytes);
}
```

```csharp
// ========================================
// Psd2UIFormConverter.cs
// ========================================

// 修改前：
internal static void EnsureNineSliceBorder(object text)
{
    string texturePath = (string)text;
    // ... 获取纹理 ...
    val.spriteBorder = UGUIParser.CalculateNineSliceBorder(val2.texture, 0);
    // ... 保存 ...
}

// ✅ 修改后：
internal static void EnsureNineSliceBorder(string texturePath, string layerName = null)
{
    // 获取纹理导入器
    TextureImporter importer = AssetImporter.GetAtPath(texturePath) as TextureImporter;
    if (importer == null) return;

    // 确保可读
    bool wasReadable = importer.isReadable;
    if (!wasReadable)
    {
        importer.isReadable = true;
        importer.SaveAndReimport();
    }

    // 加载纹理
    Texture2D texture = AssetDatabase.LoadAssetAtPath<Texture2D>(texturePath);
    if (texture == null) return;

    // ✅ 使用新的九宫算法（传递图层名）
    Vector4 border = UGUIParser.CalculateNineSliceBorder(texture, layerName);
    
    if (border != Vector4.zero)
    {
        importer.spriteBorder = border;
        importer.SaveAndReimport();

        // 可选裁剪
        if (ScriptableSingleton<Psd2UIFormSettings>.Instance.AutoCropMinimalNineSlice)
        {
            TryCropMinimalNineSlice(texturePath, layerName);
        }
    }

    // 恢复可读性
    if (!wasReadable)
    {
        importer.isReadable = false;
        importer.SaveAndReimport();
    }
}

// 新增裁剪函数（使用 PSDLayoutTool2 的裁剪器）
private static bool TryCropMinimalNineSlice(string texturePath, string layerName)
{
    Texture2D texture = AssetDatabase.LoadAssetAtPath<Texture2D>(texturePath);
    if (texture == null) return false;

    // 解析命名规则
    PsdNineSliceNameRule rule = null;
    if (!string.IsNullOrEmpty(layerName))
    {
        if (layerName.Contains("|sliced") || layerName.Contains("|九宫格"))
        {
            rule = new PsdNineSliceNameRule(PsdNineSliceMode.NineSlice, null);
        }
        else
        {
            PsdNineSliceNameRules.TryParse(layerName, out rule);
        }
    }
    
    if (rule == null)
    {
        rule = new PsdNineSliceNameRule(PsdNineSliceMode.NineSlice, null);
    }

    // 使用 PSDLayoutTool2 的裁剪处理器
    byte[] pngBytes;
    PsdNineSliceBorder border;
    string reason;
    
    if (PsdNineSliceAutoProcessor.TryProcessRaster(
        UGUIParser.TextureToRaster(texture),
        rule,
        out var croppedRaster,
        out border,
        out reason))
    {
        // 编码为 PNG
        Texture2D croppedTexture = new Texture2D(croppedRaster.Width, croppedRaster.Height, TextureFormat.RGBA32, false);
        Color32[] croppedPixels = new Color32[croppedRaster.Width * croppedRaster.Height];
        
        for (int i = 0; i < croppedPixels.Length; i++)
        {
            croppedPixels[i] = new Color32(
                croppedRaster.Pixels[i * 4 + 0],
                croppedRaster.Pixels[i * 4 + 1],
                croppedRaster.Pixels[i * 4 + 2],
                croppedRaster.Pixels[i * 4 + 3]);
        }
        
        croppedTexture.SetPixels32(croppedPixels);
        pngBytes = croppedTexture.EncodeToPNG();
        UnityEngine.Object.DestroyImmediate(croppedTexture);

        // 写入文件
        System.IO.File.WriteAllBytes(texturePath, pngBytes);
        AssetDatabase.Refresh();

        // 更新边界
        TextureImporter importer = AssetImporter.GetAtPath(texturePath) as TextureImporter;
        if (importer != null)
        {
            importer.spriteBorder = new Vector4(border.Left, border.Bottom, border.Right, border.Top);
            importer.SaveAndReimport();
        }

        Debug.Log($"[九宫裁剪] {texturePath}: 裁剪成功");
        return true;
    }
    else
    {
        Debug.LogWarning($"[九宫裁剪] {texturePath}: {reason}");
        return false;
    }
}
```

```csharp
// ========================================
// UGUIParser.cs - 修改调用点（传递图层名）
// ========================================

// 在生成 Image 组件时：
if (imageType == Image.Type.Sliced)
{
    // ✅ 传递图层名
    Psd2UIFormConverterEditor.EnsureNineSliceBorder(texturePath, layerName: layer.Name);
}
```

---

### 第四步：更新 Photoshop 脚本菜单

```javascript
// PSD2UGUI-LayerTagMenu.jsx

// ========================================
// 九宫格菜单项
// ========================================

// 旧菜单项（保留兼容）
menuItems.push({
    label: "图像类型: 九宫格 (旧格式)",
    tag: "|sliced",
    description: "向后兼容，自动转换为新算法"
});

// ✅ 新增菜单项
menuItems.push({
    label: "图像类型: 九宫格 (自动推断)",
    tag: "|9slice",
    description: "使用三重推断算法（视觉边缘+重复检测+圆角保护）"
});

menuItems.push({
    label: "图像类型: 九宫格 (显式边界 L,T,R,B)",
    tag: "|9slice=10,10,10,10",
    description: "手动指定边界值（需修改数值）"
});

menuItems.push({
    label: "图像类型: 横向三切 (进度条)",
    tag: "|h3slice",
    description: "只拉伸水平方向，保留完整高度"
});

menuItems.push({
    label: "图像类型: 纵向三切 (装饰条)",
    tag: "|v3slice",
    description: "只拉伸垂直方向，保留完整宽度"
});

menuItems.push({
    label: "图像类型: 九宫格 (中文标签)",
    tag: "|jiugong",
    description: "Figma 兼容标签"
});
```

---

### 第五步：删除 PSDLayoutTool2 目录（最终清理）

```bash
# 在合并完成并测试通过后：
rm -rf Assets/PSDLayoutTool2/
```

**最终只保留：**
```
Assets/PSD2UIForm/
├── Src/
│   ├── Editor/
│   │   ├── Psd2UGUI/
│   │   │   ├── NineSlice/          ← 从 PSDLayoutTool2 复制过来
│   │   │   │   ├── PsdNineSliceAnalyzer.cs
│   │   │   │   ├── PsdNineSliceAutoProcessor.cs
│   │   │   │   ├── PsdNineSliceCropper.cs
│   │   │   │   ├── PsdNineSliceModels.cs
│   │   │   │   ├── PsdNineSliceNameRules.cs
│   │   │   │   ├── PsdNineSliceWindow.cs
│   │   │   │   └── ... (其他九宫文件)
│   │   │   ├── Conversion/
│   │   │   │   ├── UGUIParser.cs   ← 修改调用点
│   │   │   │   └── Psd2UIFormConverter.cs ← 修改调用点
│   │   │   └── ...
│   │   └── ...
│   └── Runtime/
├── PSScript/
│   └── PSD2UGUI-LayerTagMenu.jsx  ← 更新菜单
└── Examples/
```

---

## 📝 命名空间迁移

### 批量修改命名空间

```csharp
// 从 PSDLayoutTool2 复制过来的文件，统一修改：

// 查找:
namespace PsdLayoutTool2

// 替换:
namespace UGF.EditorTools.Psd2UGUI.NineSlice

// 或者保持简洁:
namespace Psd2UGUI.NineSlice
```

### 统一 using 引用

```csharp
// UGUIParser.cs 头部添加：
using Psd2UGUI.NineSlice;  // 或 UGF.EditorTools.Psd2UGUI.NineSlice

// 然后就可以直接使用：
PsdNineSliceAnalyzer.TryInfer(...)
PsdNineSliceNameRules.TryParse(...)
```

---

## 🗂️ 文件清单

### 需要复制的文件（14 个）

| 文件 | 行数 | 说明 |
|-----|------|------|
| `PsdNineSliceModels.cs` | 105 | 数据模型（Border, Inference, Confidence） |
| `PsdNineSliceRaster.cs` | 58 | 光栅数据封装 |
| `PsdNineSliceNameRules.cs` | 165 | 命名规则解析器 |
| `PsdNineSliceAnalyzer.cs` | 371 | **核心** - 三重推断引擎 |
| `PsdNineSliceCropper.cs` | 131 | 裁剪执行器 |
| `PsdNineSliceCropSafety.cs` | 79 | 安全检查（防止过度裁剪） |
| `PsdNineSliceAutoProcessor.cs` | 150 | Unity 集成层 |
| `PsdNineSliceAutoProcessorCore.cs` | 88 | 核心处理逻辑 |
| `PsdNineSliceImportPolicy.cs` | 43 | 导入策略枚举 |
| `PsdNineSliceTextureProcessor.cs` | 309 | 纹理处理工具 |
| `PsdNineSliceWindow.cs` | 822 | 可视化编辑器（可选） |
| `PsdNineSliceOverrideStore.cs` | 207 | 手动覆盖存储（可选） |
| `PsdNineSlicePsdLayerSession.cs` | 149 | PSD 层会话（可选） |
| `PsdNineSliceAssetState.cs` | 183 | 资产状态管理（可选） |

**必需文件（前 10 个）**: ~1500 行  
**可选文件（后 4 个）**: ~1361 行（可视化编辑器功能）

### 需要修改的文件（3 个）

| 文件 | 修改点 | 工作量 |
|-----|--------|--------|
| `UGUIParser.cs` | 删除旧算法（~175行），添加新调用（~40行） | 中 |
| `Psd2UIFormConverter.cs` | 修改 `EnsureNineSliceBorder`（~50行） | 低 |
| `RightClickExtension.cs` | 修改 `TryCropMinimalNineSlice`（~30行） | 低 |

### 需要删除的目录（1 个）

```bash
Assets/PSDLayoutTool2/  ← 整个目录删除（测试通过后）
```

---

## ✅ 验证清单

### 编译检查
- [ ] Unity 编译通过，无错误
- [ ] 无命名空间冲突
- [ ] 所有 using 引用正确

### 功能测试
- [ ] 旧标签 `|sliced` 继续工作
- [ ] 新标签 `|9slice` 正常推断
- [ ] 显式边界 `|9slice=L,T,R,B` 正确应用
- [ ] 三切模式 `|h3slice`、`|v3slice` 正常工作
- [ ] 圆角矩形边界正确（目测无拉伸失真）
- [ ] 自动裁剪功能正常
- [ ] 可视化编辑器可用（如果保留）

### 回归测试
- [ ] 现有项目重新生成 UI
- [ ] 对比生成前后的 spriteBorder 值
- [ ] UI 拉伸效果正确

### 性能测试
- [ ] 1000 个图层处理时间 < 10秒
- [ ] 无明显性能回归

---

## 📊 合并收益

### 代码统计

| 维度 | PSD2UIForm | PSDLayoutTool2 | 合并后 |
|-----|-----------|----------------|--------|
| **核心代码** | ~15,000 行 | ~8,000 行 | ~16,500 行 |
| **九宫代码** | ~175 行（旧） | ~1,500 行（新） | ~1,500 行（新） |
| **总行数** | ~15,175 行 | ~8,000 行 | **~16,500 行** |

**净增长**: +1,325 行（+8.7%）  
**功能增长**: +350%（九宫标签） + 圆角保护 + 三切模式 + 可视化编辑器

### 功能对比

| 功能模块 | PSD2UIForm | PSDLayoutTool2 | 合并后 |
|---------|-----------|----------------|--------|
| **UGUI 生成** | ✅ 完整 | ❌ 无 | ✅ 保留 |
| **Photoshop 集成** | ✅ 完整 | ❌ 无 | ✅ 保留 |
| **AI 辅助布局** | ✅ 有 | ❌ 无 | ✅ 保留 |
| **九宫推断** | ⚠️ 单策略 | ✅ 三重策略 | ✅ 升级 |
| **圆角保护** | ❌ 无 | ✅ 有 | ✅ 新增 |
| **三切模式** | ❌ 无 | ✅ 有 | ✅ 新增 |
| **可视化编辑器** | ❌ 无 | ✅ 有 | ✅ 新增 |
| **命名标签** | 2 种 | 9 种 | ✅ 9 种 |

**结果**: 保留所有优势，无功能损失

---

## ⏱️ 实施时间线

### 第一阶段：文件迁移（0.5 天）
- [ ] 复制 NineSlice/ 目录（14 个文件）
- [ ] 批量修改命名空间
- [ ] 添加 .meta 文件
- [ ] 编译检查

### 第二阶段：代码替换（1 天）
- [ ] 删除 UGUIParser 旧九宫代码（~175 行）
- [ ] 修改 CalculateNineSliceBorder 实现
- [ ] 修改 EnsureNineSliceBorder 实现
- [ ] 修改 TryCropMinimalNineSlice 实现
- [ ] 追踪图层名传递路径
- [ ] 编译通过

### 第三阶段：测试验证（1-2 天）
- [ ] 创建测试 PSD（10+ 场景）
- [ ] 功能测试（旧标签、新标签、圆角、三切）
- [ ] 回归测试（现有项目）
- [ ] 性能测试
- [ ] 修复发现的问题

### 第四阶段：清理与文档（0.5 天）
- [ ] 删除 PSDLayoutTool2 目录
- [ ] 更新 Photoshop 脚本菜单
- [ ] 更新用户文档
- [ ] 更新 CLAUDE.md

**总计**: 3-4 天

---

## 🎯 最终目标

**一个统一的 PSD 导入插件**，具备：

✅ PSD2UIForm 的核心功能：
- UGUI 完整生成（Button、Image、Text、ScrollView 等）
- Photoshop 脚本集成（自动标注、一键导出）
- AI 辅助布局（Claude Code 集成）

✅ PSDLayoutTool2 的九宫技术：
- 三重推断算法（视觉边缘 + 重复检测 + 圆角保护）
- 9 种命名标签（`|9slice`、`|h3slice`、`|v3slice`、显式边界等）
- 可视化编辑器（手动调整九宫边界）
- 最小裁剪优化

**插件名称**: PSD2UIForm（保持品牌连续性）  
**代码质量**: 模块化、有文档、易维护  
**向后兼容**: 旧标签继续工作，零迁移成本

---

## 🚀 下一步

**需要你确认：**

1. ✅ 确认合并方向：以 PSD2UIForm 为基础，整合 PSDLayoutTool2 的九宫模块
2. ❓ 是否保留可视化编辑器？（`PsdNineSliceWindow.cs` + 相关 3 个文件，~1360 行）
   - 保留：可以手动调整九宫边界（推荐）
   - 不保留：节省 ~1360 行代码，但失去手动调整功能
3. ❓ 是否需要立即更新 Photoshop 脚本菜单？
4. ❓ 是否需要我创建详细的文件迁移脚本？

**我可以立即开始：**
- 创建文件复制和命名空间修改脚本
- 重写 `UGUIParser.CalculateNineSliceBorder` 实现
- 修改调用点代码

请告诉我你的决定，我马上开始实施！
