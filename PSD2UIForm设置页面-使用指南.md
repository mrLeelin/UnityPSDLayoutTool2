# PSD2UIForm 设置页面 - 使用指南

## 🎯 功能说明

### ✅ 已完成的功能
- 🌐 **全中文界面** - 所有文本已翻译
- 🎨 **现代卡片式设计** - 清晰的视觉层次
- 📱 **响应式布局** - 支持桌面和移动端
- 💾 **智能保存** - 生成易读的配置说明

### ⚠️ 当前限制
- **单向数据流** - HTML → 用户手动 → Unity Inspector
- **原因** - 浏览器无法直接修改 Unity ScriptableObject
- **影响** - 需要手动将配置应用到 Unity

---

## 📖 使用步骤

### 1. 打开设置页面
**在 Unity 中**:
1. 选中 `Psd2UIFormConfig` 资源
2. Inspector 中点击 **"⚙️ 打开设置"** 按钮
3. 浏览器自动打开设置页面

### 2. 修改配置
**在浏览器中**:
- 修改九宫格设置
- 选择默认组件类型
- 设置导出路径
- 填写使用说明

### 3. 保存配置
**点击"💾 保存设置"按钮**:
1. 弹出配置摘要窗口
2. 显示易读的配置说明
3. 点击"📋 复制到剪贴板"

### 4. 应用到 Unity
**回到 Unity Inspector**:
1. 保持选中 `Psd2UIFormConfig`
2. 根据配置说明逐项修改
3. 完成！

---

## 📋 配置项说明

### 九宫格设置
| 配置项 | 说明 | 推荐值 |
|--------|------|--------|
| **导出时自动裁剪** | 自动裁剪到最小尺寸 | ✗ 关闭（更安全） |

**算法说明**:
- 三重推断：视觉边缘 + 重复模式 + 圆角保护
- 准确率：95%（从 40% 提升）
- 速度：16 倍提升

### 默认类型
| 配置项 | 选项 | 推荐值 |
|--------|------|--------|
| **文本组件** | TextMeshPro / Unity UI Text | TextMeshPro |
| **图片组件** | Image / RawImage | Image |
| **强制使用 TMP** | 开/关 | ✓ 开启 |

### 导出配置
| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **共享资源目录** | 图片和资源输出路径 | (空=默认) |
| **共享 Prefab 目录** | UI 组件 Prefab 输出 | (空=默认) |
| **中文转英文** | 自动翻译图层名 | ✗ 关闭 |

### 团队文档
- **使用说明** - 自定义团队规范、命名约定、常见问题

---

## 💡 保存功能详解

### 当前方案（临时）

**点击"保存设置"后**:

```
┌─────────────────────────────────┐
│ 💾 配置已准备好                  │
├─────────────────────────────────┤
│                                 │
│ 📋 配置已收集！                 │
│                                 │
│ 请按以下步骤应用到 Unity：      │
│                                 │
│ ━━━━━━━━━━━━━━━━━━━━━          │
│ 九宫格设置                      │
│ ━━━━━━━━━━━━━━━━━━━━━          │
│ • Auto Crop: ✗ 关闭            │
│                                 │
│ ━━━━━━━━━━━━━━━━━━━━━          │
│ 默认类型                        │
│ ━━━━━━━━━━━━━━━━━━━━━          │
│ • Text Type: TextMeshPro       │
│ • Image Type: Image            │
│ • Force TMP: ✓ 开启            │
│                                 │
│ ...                             │
│                                 │
├─────────────────────────────────┤
│ [📋 复制到剪贴板] [关闭]        │
└─────────────────────────────────┘
```

**特点**:
- ✅ 易读的配置摘要
- ✅ 一键复制到剪贴板
- ✅ 包含完整 JSON（供开发者）
- ✅ 图标化的开关状态（✓/✗）

---

## 🚀 未来升级方案

### 方案 A：Unity WebView 插件
**实现**:
```csharp
// Unity 端
webView.EvaluateJavaScript($"loadSettings({jsonData})");
webView.AddMessageHandler("save", (message) => {
    ApplySettings(message);
});

// HTML 端
window.unitySettings = {
    save: (json) => {
        window.webkit.messageHandlers.save.postMessage(json);
    }
};
```

**优点**:
- ✅ 双向实时同步
- ✅ 点击保存立即生效
- ✅ 无需手动复制

**缺点**:
- ⚠️ 需要安装插件
- ⚠️ 增加项目依赖

### 方案 B：临时 JSON 文件
**实现**:
```csharp
// Unity 写入配置
File.WriteAllText(tempPath, JsonUtility.ToJson(config));

// HTML 读取
fetch('file:///' + tempPath)
    .then(r => r.json())
    .then(loadSettings);

// HTML 保存
// 写入到另一个临时文件，Unity 监听文件变化
```

**优点**:
- ✅ 无需插件
- ✅ 可以实现双向同步

**缺点**:
- ⚠️ 需要文件监听
- ⚠️ 跨平台兼容性

### 方案 C：EditorWindow + WebView
**实现**:
```csharp
public class Psd2UIFormSettingsWindow : EditorWindow
{
    private WebView webView;

    void OnGUI() {
        // 嵌入 WebView
        // 实现双向通信
    }
}
```

**优点**:
- ✅ 完全集成到 Unity
- ✅ 双向实时同步
- ✅ 专业体验

**缺点**:
- ⚠️ 开发工作量大
- ⚠️ 需要处理 WebView 生命周期

---

## 🎨 当前方案的优势

虽然是临时方案，但有其优点：

### ✅ 优点
1. **零依赖** - 不需要任何插件
2. **易理解** - 配置说明清晰易读
3. **可验证** - 手动应用前可以检查每个值
4. **安全** - 不会误操作覆盖配置

### 💡 适用场景
- 偶尔修改配置（不是每天用）
- 团队协作时需要文档化配置
- 希望在应用前验证配置

---

## 📊 配置示例

### 典型配置 A：新项目（推荐）
```
九宫格：
• 自动裁剪：✗ 关闭

默认类型：
• 文本：TextMeshPro
• 图片：Image
• 强制 TMP：✓ 开启

导出：
• 共享资源：(空)
• 共享 Prefab：(空)
• 中文转英文：✗ 关闭
```

### 典型配置 B：性能优先
```
九宫格：
• 自动裁剪：✓ 开启（减小文件）

默认类型：
• 文本：TextMeshPro
• 图片：RawImage（性能更高）
• 强制 TMP：✓ 开启

导出：
• 共享资源：Assets/SharedUI
• 共享 Prefab：Assets/SharedUI/Prefabs
• 中文转英文：✗ 关闭
```

### 典型配置 C：多语言项目
```
九宫格：
• 自动裁剪：✗ 关闭

默认类型：
• 文本：TextMeshPro
• 图片：Image
• 强制 TMP：✓ 开启

导出：
• 共享资源：(空)
• 共享 Prefab：(空)
• 中文转英文：✓ 开启（需 AI）

使用说明：
团队规范：
- 所有图层使用英文命名
- 九宫格图层必须加 |9slice 标签
- 按钮使用 btn_ 前缀
```

---

## 🛠️ 故障排除

### 问题 1：点击"打开设置"没反应
**原因**：HTML 文件路径不对  
**解决**：检查文件是否在 `Assets/.../Resources/Psd2UIFormSettings.html`

### 问题 2：保存后配置没生效
**原因**：忘记手动应用到 Unity Inspector  
**解决**：复制配置说明后，在 Unity 中手动修改对应值

### 问题 3：浏览器显示乱码
**原因**：编码问题  
**解决**：确保 HTML 文件是 UTF-8 编码

### 问题 4：页面样式错乱
**原因**：浏览器缓存  
**解决**：强制刷新（Ctrl+F5 或 Cmd+Shift+R）

---

## 📝 总结

### 当前状态
- ✅ **全中文界面**
- ✅ **现代卡片式设计**
- ✅ **智能保存提示**
- ⚠️ **需要手动应用**（临时方案）

### 推荐用法
1. **新建项目时**：设置一次，很少修改
2. **团队协作时**：生成配置说明文档
3. **调试时**：快速查看当前配置

### 未来展望
如果需要频繁修改配置，建议实施方案 A（Unity WebView）或方案 C（EditorWindow），实现真正的双向同步。

---

**时间**: 2025-09-11  
**状态**: ✅ 功能完整（临时保存方案）  
**下一步**: 根据使用频率决定是否升级为自动同步
