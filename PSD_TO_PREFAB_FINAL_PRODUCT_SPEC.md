# PSD → Prefab 最终产品改造规格

## 1. 产品目标

将当前 `UnityPSDLayoutTool2` 改造成面向生产使用的 PSD → Unity Prefab 工具，核心目标是：

> 设计师将 PSD 放入 Unity 后，可以一键生成可用 Prefab；PSD 修改后可以安全增量更新；复杂 UI 能通过九宫推断、AI 图集整理和公共组件复用保持可维护性。

产品必须同时满足视觉还原、结构可维护、重复导入安全和项目资源可复用四个目标。

## 2. 五项核心能力

### 2.1 PSD 一键无损转 Prefab

用户选中 PSD 后，通过 Inspector 的单一主入口完成：

```text
读取 PSD
→ 解析图层与文本
→ 导出或复用图片资源
→ 生成 Unity 层级
→ 设置 Canvas、RectTransform、Anchor、透明度和渲染顺序
→ 生成 Prefab
→ 自动验证并输出报告
```

“无损”定义为：

- 图层名称、层级、顺序和可见性正确；
- 中文图层名和中文文本不乱码；
- 图片位置、尺寸、旋转、透明度和混合效果正确；
- 文本内容、字体、字号、行高、对齐和颜色正确；
- 蒙版 Alpha 与图层 Opacity 正确叠加；
- Unity UI 与 World Space 两种模式均可用；
- 生成的 Prefab 可被 Unity 正常加载，不产生空 Sprite 引用；
- 生成过程不覆盖用户已有业务脚本、组件和序列化绑定。

无损不是只比较文件是否生成成功，必须支持 Unity 实际渲染截图或等价运行时证据。

### 2.2 安全增量更新

PSD 修改后，工具必须识别变化并只更新受 PSD 所有权控制的内容：

- 未变化图层：跳过；
- 内容变化图层：只替换像素或文本内容；
- 位置/尺寸变化图层：更新对应 RectTransform；
- 新增图层：加入更新计划，确认后创建；
- PSD 删除图层：默认标记为待处理，不直接删除可能包含业务绑定的 Unity 节点；
- 图层改名：优先使用稳定图层 ID 匹配，不因改名而误创建重复节点。

增量更新必须具备：

1. Preview：只读展示新增、修改、缺失和冲突。
2. Fingerprint：确认前重新检查 PSD、目标 Prefab 和资源是否发生变化。
3. Apply：只写入 PSD 拥有字段。
4. Verify：验证层级、资源引用、源状态和受保护组件。
5. Rollback：失败时恢复旧状态并报告回滚结果。

任何身份冲突、资源缺失、预览过期或验证失败都必须阻断 Apply，不能静默猜测或覆盖。

### 2.3 九宫推断

工具需要从 PSD 图层名称、尺寸、边缘结构和内容特征中推断九宫格候选，并允许用户在 Preview 阶段修正。

支持：

- 标准 `9slice`；
- 横向 `h3slice`；
- 纵向 `v3slice`；
- 普通图片降级；
- 显式命名参数优先于自动推断；
- 推断结果包含 border、slice 类型、来源和置信度。

规则：

- 小尺寸或不适合拉伸的图片不得强制九宫；
- 九宫源图、可见尺寸、border 和 SpriteImporter 设置必须分别记录；
- 生成的九宫 PNG 必须满足对应最小尺寸；
- 隐藏源图不能因为 `visible=false` 被错误跳过；
- 推断置信度不足时进入人工确认，不得静默生成错误九宫；
- 九宫更新必须验证 PNG 尺寸、border、Sprite 模式和 Prefab 引用。

### 2.4 AI 整理图集

工具需要支持对 PSD 导出的图片资源进行 AI 辅助整理，但 AI 只负责提出结构化计划，确定性代码负责执行。

AI 可协助：

- 识别重复图片、近似图片和同一功能下的图片；
- 建议图集分组、命名和目录；
- 识别可能的按钮状态、列表项状态和公共资源；
- 建议哪些图片适合合并为图集；
- 标记疑似九宫格、背景、装饰、图标和可复用组件；
- 输出需要人工确认的风险项。

AI 不得：

- 直接删除源文件；
- 直接覆盖已有公共资源；
- 仅凭相似度替换业务组件；
- 修改 Prefab 业务脚本或 SerializeField 绑定；
- 绕过 Preview/Apply/Verify 事务；
- 把 AI 推断当作确定事实。

AI 整理流程：

```text
生成资源清单
→ AI 分析与提出计划
→ 展示分组、复用、移动、合并建议
→ 用户确认
→ 确定性执行器应用
→ 资源引用扫描
→ Prefab 重新验证
```

所有 AI 建议必须记录原因、置信度、影响文件和可回滚信息。

### 2.5 公共组件复用

工具需要识别并复用项目已有公共 Prefab、Sprite、材质和字体，避免每次 PSD 导入都生成重复资源。

复用优先级：

1. 显式用户指定的公共资源；
2. 稳定资源 ID 或项目索引命中；
3. 精确名称和尺寸匹配；
4. 内容 hash 或视觉 hash 匹配；
5. AI 建议候选，但必须经过确认。

公共资源规则：

- 命中公共 Prefab 后默认以 PrefabInstance 引用，不拆开重建；
- 公共 PNG、材质、字体和 Prefab 不得静默覆盖；
- 公共资源内部的脚本、字段、RaycastTarget 和层级保持源状态；
- 复用结果必须记录 `sourcePath`、GUID、匹配依据和置信度；
- 未命中时才生成当前功能内的新资源；
- 复用失败必须降级为普通资源并记录原因，不得错误替换。

## 3. 产品入口与用户体验

Inspector 至少提供以下入口：

- `一键生成 Prefab`
- `分析 PSD 增量变化`
- `确认并应用增量更新`
- `重新采用当前 Prefab 为基线`
- `推断九宫格`
- `AI 整理图集`
- `查看最新诊断报告`

首次导入可一键执行；已有 Prefab 的更新必须先进入 Preview。危险操作必须展示目标路径、影响节点、资源变更和回滚能力。

## 4. 数据与身份契约

每个 PSD 图层应形成稳定的源状态：

```json
{
  "version": 1,
  "layerId": "stable-psd-layer-id",
  "mode": "image",
  "parentLayerId": "...",
  "order": 0,
  "geometry": { "x": 0, "y": 0, "width": 0, "height": 0, "rotation": 0 },
  "display": { "visible": true, "opacity": 1, "blendMode": null },
  "content": { "contentHash": "sha256:...", "assetKey": "..." },
  "text": null,
  "nineSlice": null,
  "component": null
}
```

身份匹配优先级：

```text
稳定 PSD 图层 ID
→ 已保存的源状态身份
→ 受控迁移规则
→ 冲突并阻断
```

禁止仅凭名称、数组索引或层级路径自动决定长期身份。

## 5. 资源与 Prefab 产物

一次完整导入至少产生或更新：

- PNG/Sprite 资源；
- 对应 `.meta`；
- Prefab；
- 可选 `.anim`/Animator 资源；
- 源状态和导入摘要；
- 诊断日志与验证报告。

不得提交 Unity `Library/`、临时缓存、AI 原始会话和未确认的中间文件。

Prefab 生成要求：

- 只能通过确定性生成器创建，禁止手写 Prefab YAML；
- 子节点坐标必须是相对直接父节点的坐标；
- 图片默认关闭 `RaycastTarget`，除非明确配置为交互节点；
- TMP 默认关闭 AutoSize 并绑定项目公共字体/材质策略；
- 业务脚本和序列化字段不自动推断、不自动覆盖；
- 所有 Sprite 引用必须可加载，空 Sprite 是阻断错误。

## 6. 最终验收标准

### 一键导入

- [ ] 选中 PSD 后可以通过一个主按钮生成 Prefab。
- [ ] 中文、透明度、蒙版、隐藏层、重名层和文本通过真实素材验证。
- [ ] Prefab 能在 Unity 中加载并实际渲染。

### 增量更新

- [ ] 不变图层不重复生成资源。
- [ ] 改名、移动、改尺寸和改内容能正确匹配。
- [ ] 新增、缺失、重复身份和资源冲突有明确 Preview。
- [ ] 预览过期会阻断 Apply。
- [ ] Apply 失败后无半成品，回滚结果可查询。

### 九宫推断

- [ ] 支持 9slice、h3slice、v3slice 和普通图片降级。
- [ ] 推断结果显示 border、类型、置信度和警告。
- [ ] PNG 尺寸、SpriteImporter 和 Prefab 引用全部通过验证。

### AI 图集整理

- [ ] AI 只输出结构化计划，不直接执行破坏性操作。
- [ ] 用户确认后才能移动、合并或复用资源。
- [ ] 整理后所有 Prefab 引用仍然有效。
- [ ] AI 建议可追踪、可解释、可回滚。

### 公共组件

- [ ] 能发现并优先复用公共 Prefab、Sprite、材质和字体。
- [ ] 公共资源不会被静默覆盖。
- [ ] 复用关系和匹配依据有报告。
- [ ] 未命中时才生成新资源。

## 7. 实施顺序

1. 先建立 PSD 稳定身份和 sourceState 数据模型。
2. 实现一键导入的统一流水线和诊断报告。
3. 实现 Preview/Diff/Fingerprint/Apply/Rollback。
4. 接入九宫推断和 Sprite 资源验证。
5. 建立公共资源索引与 PrefabInstance 复用。
6. 最后接入 AI 图集整理，并将 AI 限制在计划和建议层。
7. 使用真实 Unity 项目、真实 PSD 和截图完成最终验收。

在第 1～3 步完成并通过回归测试前，不应大规模重写现有 `PsdImporter` 主流程。
