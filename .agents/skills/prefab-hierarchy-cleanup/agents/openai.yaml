interface:
  display_name: "Prefab Hierarchy Cleanup"
  short_description: "Organize existing Unity Prefabs safely"
  default_prompt: "Use $prefab-hierarchy-cleanup to inspect this Prefab, organize its existing hierarchy in place with semantic names and containers, and wait for my confirmation before applying it."

policy:
  allow_implicit_invocation: false
