interface:
  display_name: "Prefab Hierarchy Cleanup"
  short_description: "Organize existing Unity Prefabs safely"
  default_prompt: "Use $prefab-hierarchy-cleanup to inspect this Prefab, replace PSD/export names with semantic names, propose shared stateful components where justified, and wait for my confirmation before applying it."

policy:
  allow_implicit_invocation: false
