# ADR 0001：v2 作为唯一正式清理计划协议

## 状态
已接受。

## 决策
Prefab 层级清理的正式执行链只接收 version 2 计划，并在 Unity 内解析为强类型、已验证的执行对象。v1 仅可作为显式只读历史迁移或诊断输入，不能直接 Apply。

## 原因
节点 ID、快照指纹、阶段验证和后续抽取意图必须保持同一份语义。v2 到 v1 的转换会产生第二套可执行语义，导致 Chat、CLI 与 Unity 的校验边界漂移。

## 约束
Apply 不确定时只能进入只读 Verify；不得自动重试、恢复或重建计划。
