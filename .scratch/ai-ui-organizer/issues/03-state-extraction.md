# 03: 结构差异组件的公共部分和状态抽取

**Status:** done

**Blocked by:** 无（02已完成）。

**Scope:** 用户已要求继续实现本工单。沿用手动预览/应用入口，支持普通与特殊结构共用公共 Prefab并保留默认显示形态；本次不新增运行时切换组件。统一AI入口和源更新合并仍归后续工单。

交付：普通奖励和特殊多奖励组件共用一个包含公共部分及状态分组的组件，各实例保留默认形态。

- [x] 预览列出有序公共成员、状态成员及实例对应状态。
- [x] 应用后各实例保留原可见属性，重复生成后状态映射不丢失。
- [x] 本次仅保存默认状态；运行时状态切换组件不在本工单范围。

## 本次实施与验证边界

沿用01/02的公开 Preview/Apply 和保存后重载资产边界验证。相同结构保留原抽取方式；新增显式的差异结构模式，预览列出公共成员、各分支成员和每个来源的默认分支。只有能证明顺序、布局和引用安全的成员才共享，否则保留在原分支。签到素材只作验收例子，不在算法内使用其名字作规则。

## 交付与验收（2026-09-14）

- 原抽取窗口增加“不同结构（保留各自形态）”选项，公共 Prefab 包含 Common 和 States 分组。
- 为保留绘制顺序，只合并开头连续匹配的共有子树。根组件不同则保留完整分支；根自动布局等不支持的输入在预览时拒绝。
- Unity 编译通过；插件全量 EditMode 测试（包含显式验收）41/41通过，93.32秒。
- 新增5项状态抽取测试，包括七日签到成品 Prefab 的七个奖励区重新抽取。修复并回归验证了共有成员移动引起的显示顺序变化、承载节点重复叠加Z偏移。
- 验证图片资源、颜色、位置、绘制顺序、TMP文字、按钮内部引用及同输入规则复用。未执行截图像素比对或任意自定义运行时脚本验收。
- [使用说明与验证边界](../acceptance/state-extraction.md)。统一AI入口与源更新合并仍属于后续工单。
